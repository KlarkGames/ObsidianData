# Вывод данных
#Pandas 

Пусть у нас есть некоторая таблица A:

```jupyter
import pandas as pd
A = pd.DataFrame([
	["Семфирополь", 10, 15.02.2007],
	["Москва", 11, 15.02.2007],
	["Лондон", 12, 20.09.2014],
	["Сидней", 13, 31.12.2021],
	["Токио", 12, 11.02.2015],
	["Прага", 11, 03.03.2010],
	["Перу", 10, 21.07.2016],
])

A.head() #Возвращает первые 5 элементов

#A.head(3) #Возвращает первые 3 элемента

#A.tail() #Возвращает последние 5 элементов
#A.tail(3) #Возвращает последние 3 элемента

#A.index() #Возвращает индексы и их типы
#A.columns() #Возвращает столбцы и их типы

#A.to_numpy() #Возвращает таблицу в виде numpy array
# Является очень дорогой операцией при приведении таблиц
# с множеством различных данных разных типов, т.к. 
# numpy array имеет один тип данных для всей таблицы.

#A.describe() #Возвращает краткую статистику по базе



```